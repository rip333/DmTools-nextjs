import React from 'react';
import styles from "./styles.module.css";

function Footer() {
    return (
        <div className={styles.footer}>
           this is the footer. or is it? yes it is. or...is it? no no, it really is.
        </div>
    );
}

export default Footer;